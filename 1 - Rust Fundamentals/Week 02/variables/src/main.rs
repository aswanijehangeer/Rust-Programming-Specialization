fn main() {
    // variables
    let message = "Name: Jehangeer, Weight: ";
    let weight = 153.0;

    let kilos = weight / 2.2;
    println!("{}{}", message, weight);
    println!("{}{}", message, kilos);
}
